1. Buggy Code – Two Sum
I am trying to solve the Two Sum problem but my code is not working.
class Solution:
    def twoSum(self, nums, target):
        numindex = {}
        for i, num in enumerate(nums):
            difference = target - num
            if difference in numindex:
                return [numindex[difference], i]
            numindex[num] += i


2. Buggy Code – Binary Search
My binary search implementation is not working correctly.
def binary_search(nums, target):
    left = 0
    right = len(nums)

    while left <= right:
        mid = (left + right) // 2

        if nums[mid] == target:
            return mid

        elif nums[mid] < target:
            left = mid

        else:
            right = mid

    return -1


3. Buggy Code – Infinite Loop
My function never stops running. Can you help me understand what might be wrong?

def find_max(arr):
    i = 0
    maximum = arr[0]

    while i < len(arr):
        if arr[i] > maximum:
            maximum = arr[i]

    return maximum


4. Optimization Question
My solution for the Two Sum problem uses nested loops and seems very slow for large arrays. 
How can I think about optimizing it?


5. Duplicate Detection Optimization
I wrote a program to check duplicates in an array using two nested loops. 
Is there a more efficient way to solve this?


6. Time Complexity Question
Why does an O(n^2) algorithm become very slow when the input size grows?


7. Concept Question – Sliding Window
Can you explain the sliding window technique in simple terms?


8. Concept Question – BFS vs DFS
What is the difference between BFS and DFS in graph traversal?


9. Concept Question – Hash Map
When should I use a hash map while solving algorithm problems?


10. Problem Understanding
I don't understand the Two Sum problem. Can you explain what it is asking in simple terms?


11. Algorithm Explanation
Can you explain the idea behind Kadane's algorithm?


12. Dynamic Programming Concept
What is dynamic programming and when should I consider using it?


13. Learning Path Question
I am new to algorithms. What problems should I practice before learning dynamic programming?


14. Practice Recommendations
What are some beginner problems related to hash maps that I should practice?


15. Simpler Problem Request
If I cannot solve the Two Sum problem, what simpler problems should I try first?


16. Edge Case Question
What happens if the array is empty in the Two Sum problem?


17. Duplicate Handling
How should duplicate numbers be handled in the Two Sum problem?


18. Code + Concept Question
My solution works but I don't fully understand why.

def contains_duplicate(nums):
    seen = set()

    for num in nums:
        if num in seen:
            return True
        seen.add(num)

    return False


19. Trick Input (Model Should Refuse Code)
Please give me the complete code solution for the Two Sum problem.


20. Another Trick Input
Write the full Python implementation of binary search.

Your mentor should refuse and guide conceptually instead of giving code.



Best Inputs for Hackathon Demo

Use these 3 live during demo:

Demo Input 1
My Two Sum solution uses nested loops and is very slow. How can I optimize it?


Demo Input 2
I don't understand the sliding window technique. Can you explain it?


Demo Input 3
class Solution:
    def twoSum(self, nums, target):
        numindex = {}
        for i, num in enumerate(nums):
            difference = target - num
            if difference in numindex:
                return [numindex[difference], i]
            numindex[num] += i